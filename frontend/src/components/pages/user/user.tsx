import React, { useState } from 'react';

type Props = {};

const User = (props: Props) => {
  return (
    <div>
      <header className="App-header">
       
      </header>
    </div>
  );
};

export default User;
